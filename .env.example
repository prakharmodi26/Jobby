# ─── Database ────────────────────────────────────────────────
# These are used by docker-compose for the postgres service.
# Change these if you want different DB credentials.
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=myjobboard

# Connection string. For Docker Compose, the host is "postgres" (the service name).
# For local dev, use "localhost".
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/myjobboard

# ─── Auth ────────────────────────────────────────────────────
# Change this to a long random string in production!
JWT_SECRET=change-this-to-a-random-secret-string

# ─── JSearch API (RapidAPI) ──────────────────────────────────
# Required for fetching job listings. Get a key at https://rapidapi.com/letscrape-6bRBa3QguO5/api/jsearch
JSEARCH_API_KEY=your_jsearch_api_key_here

# ─── Cover Letter / AI (pick at least one) ───────────────────
# VT ARC LLM API
VT_ARC_KEY=

# Google Gemini API
GOOGLE_API_KEY=

# OpenRouter API (works with many free models)
OPENROUTER_API_KEY=

# ─── URLs ────────────────────────────────────────────────────
# Frontend origin (for CORS). Use your domain in production.
FRONTEND_URL=http://localhost:3000

# Backend API URL that the frontend calls. Used at build time (NEXT_PUBLIC_*).
NEXT_PUBLIC_API_URL=http://localhost:4000
